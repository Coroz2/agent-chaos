schema_version: 1
name: api-429-recovery

dependency:
  type: http
  base_url: http://127.0.0.1:19105
  start:
    command: [python, fake_api.py, --port, "19105"]
    cwd: ..
    readiness:
      path: /health

workload:
  name: demo-agent
  command: [python, demo_agent.py]
  cwd: ..
  proxy_url_env: CUSTOMER_API_URL

fault:
  type: http_rate_limit
  target:
    method: GET
    path: /customer/*
  trigger:
    occurrence: 2
  retry_after_seconds: 1

success:
  exit_code: 0

timeout_seconds: 10
